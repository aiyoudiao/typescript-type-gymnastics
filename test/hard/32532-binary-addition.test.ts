import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/hard/32532-binary-addition'

describe('32532 - binary-addition(二进制加法)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
