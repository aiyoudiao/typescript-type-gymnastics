import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/hard/30178-unique-items'

describe('30178 - unique-items(独特物品)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
