import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/hard/14188-run-length-encoding'

describe('14188 - run-length-encoding(游程编码)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
