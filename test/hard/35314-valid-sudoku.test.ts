import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/hard/35314-valid-sudoku'

describe('35314 - valid-sudoku(你选择数独)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
