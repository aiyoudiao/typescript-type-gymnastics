import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/hard/31824-length-of-string-3'

describe('31824 - length-of-string-3(字符串 3 的长度)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
