import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/hard/06141-binary-to-decimal'

describe('06141 - binary-to-decimal(二进制转十进制)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
