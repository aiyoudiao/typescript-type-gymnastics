import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/hard/34286-take-elements'

describe('34286 - take-elements(采取元素)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
