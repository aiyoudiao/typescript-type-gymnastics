import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/hard/09160-assign'

describe('09160 - assign(分配)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
