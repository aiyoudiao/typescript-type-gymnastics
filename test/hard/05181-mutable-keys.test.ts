import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/hard/05181-mutable-keys'

describe('05181 - mutable-keys(可变键)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
