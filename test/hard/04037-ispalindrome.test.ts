import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/hard/04037-ispalindrome'

describe('04037 - ispalindrome(回文)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
