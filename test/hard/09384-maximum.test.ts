import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/hard/09384-maximum'

describe('09384 - maximum(最大限度)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
