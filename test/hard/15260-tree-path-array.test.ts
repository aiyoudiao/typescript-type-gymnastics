import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/hard/15260-tree-path-array'

describe('15260 - tree-path-array(树形路径数组)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
