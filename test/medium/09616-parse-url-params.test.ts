import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/09616-parse-url-params'

describe('09616 - parse-url-params(解析url参数)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
