import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/01130-replacekeys'

describe('01130 - replacekeys(替换键)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
