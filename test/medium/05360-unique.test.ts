import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/05360-unique'

describe('05360 - unique(独特的)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
