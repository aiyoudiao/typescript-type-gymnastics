import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/05117-without'

describe('05117 - without(去除数组指定元素)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
