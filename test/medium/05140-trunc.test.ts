import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/05140-trunc'

describe('05140 - trunc(树干)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
