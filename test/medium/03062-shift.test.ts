import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/03062-shift'

describe('03062 - shift(转移)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
