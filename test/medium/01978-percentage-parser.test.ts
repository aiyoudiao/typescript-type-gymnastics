import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/01978-percentage-parser'

describe('01978 - percentage-parser(百分比解析器)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
