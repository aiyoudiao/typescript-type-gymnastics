import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/02688-startswith'

describe('02688 - startswith(始于)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
