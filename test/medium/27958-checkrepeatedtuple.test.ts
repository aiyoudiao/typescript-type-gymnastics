import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/27958-checkrepeatedtuple'

describe('27958 - checkrepeatedtuple(检查重复元组)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
