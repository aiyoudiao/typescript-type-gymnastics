import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/30970-shitariteraru'

describe('30970 - shitariteraru(降低)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
