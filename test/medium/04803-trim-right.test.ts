import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/04803-trim-right'

describe('04803 - trim-right(右修剪)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
