import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/08987-subsequence'

describe('08987 - subsequence(子序列)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
