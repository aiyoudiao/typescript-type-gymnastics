import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/09286-firstuniquecharindex'

describe('09286 - firstuniquecharindex(第一个唯一字符索引)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
