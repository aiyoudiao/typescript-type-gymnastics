import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/07544-construct-tuple'

describe('07544 - construct-tuple(构造元组)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
