import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/34007-compare-array-length'

describe('34007 - compare-array-length(比较数组长度)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
