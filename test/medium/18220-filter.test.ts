import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/18220-filter'

describe('18220 - filter(筛选)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
