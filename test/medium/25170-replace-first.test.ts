import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/25170-replace-first'

describe('25170 - replace-first(首先替换)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
