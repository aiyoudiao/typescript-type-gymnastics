import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/medium/01097-isunion'

describe('01097 - isunion(伊斯尤尼翁)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
