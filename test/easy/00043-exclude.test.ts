import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/easy/00043-exclude'

describe('00043 - exclude(实现 Exclude)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
