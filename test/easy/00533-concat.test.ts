import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/easy/00533-concat'

describe('00533 - concat(连接)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
