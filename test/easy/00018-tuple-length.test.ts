import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/easy/00018-tuple-length'

describe('00018 - tuple-length(获取元组长度)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
