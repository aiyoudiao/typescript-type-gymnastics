import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/easy/00189-awaited'

describe('00189 - awaited(等待)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
