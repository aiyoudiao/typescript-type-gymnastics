import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/easy/03312-parameters'

describe('03312 - parameters(参数)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
