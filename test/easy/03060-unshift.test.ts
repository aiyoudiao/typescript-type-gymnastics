import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/easy/03060-unshift'

describe('03060 - unshift(取消移位)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
