import { describe, it, expectTypeOf } from 'vitest'
import type { ReplaceMe } from '../../src/extreme/00741-sort'

describe('00741 - sort(种类)', () => {
  it('example test', () => {
    expectTypeOf<ReplaceMe>().toEqualTypeOf<any>()
  })
})
